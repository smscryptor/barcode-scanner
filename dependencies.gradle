ext {

    compileSDK = 26

    androidBuildVersion = "27.0.1"
    projectMaxSdkVersion = 22
    projectMinSdkVersion = 21

    junitVersion = '4.12'

    testDependencies = [
            junit: "junit:junit:$junitVersion",
    ]
}
